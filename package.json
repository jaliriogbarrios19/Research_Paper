{
  "name": "research-and-paper",
  "version": "0.1.2",
  "description": "Search academic databases and generate APA 7 papers with AI",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.cjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.cjs production"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "^0.24.0",
    "obsidian": "^1.5.0",
    "typescript": "^5.4.0"
  }
}
